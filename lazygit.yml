os:
  editCommand: 'floaterm'

git:
  paging:
    colorArg: always
    pager: git-split-diffs --color

  branchLogCmd: "git log --graph --pretty='%C(yellow)%h %C(green) %s %C(magenta)%d %n%C(cyan) %an  %C(cyan)%ar%n'" 

gui:
  showIcons: true
  sidePanelWidth: 0.22
