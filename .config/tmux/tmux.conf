# set -g default-terminal "screen-256color" # for mac os

# set prefix to control a
set -g prefix C-a

# reload config with r
unbind r
bind r source-file ~/.config/tmux/tmux.conf \; display "Reloaded config"

set-option -g focus-events on
set -g escape-time 0         # zero-out escape time delay
set -g mouse on 
set-option -g display-time 4000
set -g display-time 0
set -g allow-passthrough on
set -g detach-on-destroy off  # don't exit from tmux when closing a session
bind-key x kill-pane # skip "kill-pane 1? (y/n)" prompt

# Tools
bind-key "t" display-popup -E -w 40% "sesh connect \"$(sesh list -i | gum filter --limit 1 --placeholder 'Pick a sesh' --prompt=' ⚡ ' --no-strip-ansi)\""
bind g new-window -c "#{pane_current_path}" lazygit 
bind G display-popup -E -xC -yC -w 90% -h 90% -d "#{pane_current_path}" lazygit
unbind-key p 
bind p display-message -F "#{pane_current_path}"
bind Y display-popup -E -w70% -h70% -d '#{pane_current_path}' "yazi"
bind y new-window -c "#{pane_current_path}" "yazi" 

# COPY MODE
bind Enter copy-mode  # use prefix-enter to enter copy-mode
bind -n M-Enter copy-mode
set -g mode-style bg=default,fg=yellow
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with "v"
bind-key -T copy-mode-vi 'y' send -X copy-selection # copy text with "y"
unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode when dragging with mouse
set -g set-clipboard on      # use system clipboard

# STATUSLINE
set -g status 2
set -g status-format[1] ""
set -g status-position top
set -g status-left ""
set -g status-style 'bg=default' # default -> transparent
set -g status-right '#[fg=grey]  #(git -C "#{pane_current_path}" rev-parse --abbrev-ref HEAD 2>/dev/null | awk '\''{print " " $0}'\'')  #[fg=blue]#[bg=blue, fg=color232, bold]  #S #[fg=blue, bg=default]'
set -g status-interval 2

# STATUS MESSAGE
set -g message-command-style bg=default,fg=yellow
set -g message-style bg=default,fg=yellow

# WINDOW
set -g window-status-format ' #[fg=grey]  #I:#W  '
set -g window-status-current-format ' #[fg=blue]  #I:#{?window_zoomed_flag, ,}#W* '
set -g base-index 1          # start indexing windows at 1 instead of 0
set -g renumber-windows on   # renumber all windows when any window is closed
bind-key ";" last-window

# MENU
set -g menu-border-style "fg=brightcyan bg=default"
set -g menu-style "fg=blue bg=default"
set -g menu-selected-style "fg=white bg=black"

# POPUPS
set -g popup-border-style "fg=blue bg=default"

# PANE
set -g pane-active-border-style 'fg=blue,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'
set -g pane-border-format "[#[fg=white]#{?pane_active,#[bold],} :#P: #T #[fg=default,nobold]]"
bind -r j resize-pane -D 5 # resize with Prefix and multiple jk
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"


# TPM plugin
# prefix + I to install plugin
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator' # <ctrl>+hjkl between panes
# set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'


# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
